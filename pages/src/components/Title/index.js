import React from "react"
import styles from './.module.css'

export default function Title () {
    return (
    <div className={styles.box}>
            <h2 className={styles.title}>Recompensas One Piece</h2>
        </div>
    )
}